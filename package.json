{
  "name": "synapse-ai-chat",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:deploy": "npm run deploy --workspace=@synapse/backend -- --cmd 'npm run build --workspace=@synapse/web'",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
